---
---

<nav id="main-nav" class="nav">
  <div class="nav-inner">
    <a href="#top" class="nav-logo" aria-label="Aman Goyal home">
      <img src="/aman-portfolio-pfp.jpg" alt="Aman Goyal" class="nav-avatar" />
      <span class="nav-logo-text">
        <span class="font-syne">Aman Goyal</span>
        <span class="font-mono">DevOps & Backend</span>
      </span>
    </a>
    <div class="nav-right">
      <div class="nav-links">
        <a href="#work">Work</a>
        <a href="#experience">Experience</a>
        <a href="#contact">Contact</a>
      </div>
      <a href="/Aman_Goyal_Resume.pdf" download="Aman_Goyal_Resume.pdf" class="nav-resume font-mono">
        Resume ↗
      </a>
    </div>
  </div>
</nav>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 22px 0;
    border-bottom: 1px solid transparent;
    transition: padding 0.3s ease, background 0.3s ease, border-color 0.3s ease, backdrop-filter 0.3s ease;
  }

  .nav.nav-scrolled {
    padding: 14px 0;
    background: rgba(8, 8, 8, 0.82);
    backdrop-filter: blur(18px);
    -webkit-backdrop-filter: blur(18px);
    border-bottom-color: rgba(255, 255, 255, 0.06);
  }

  .nav-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 28px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .nav-logo {
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 12px;
  }

  .nav-avatar {
    width: 46px;
    height: 46px;
    object-fit: cover;
    border-radius: 50%;
    border: 1px solid rgba(232, 92, 44, 0.32);
    box-shadow: 0 10px 30px rgba(232, 92, 44, 0.14);
  }

  .nav-logo-text {
    display: flex;
    flex-direction: column;
    gap: 1px;
  }

  .nav-logo-text span:first-child {
    font-size: 0.98rem;
    font-weight: 800;
    color: #f5f5f5;
    letter-spacing: -0.02em;
    line-height: 1.05;
  }

  .nav-logo-text span:last-child {
    font-size: 0.58rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: rgba(237, 237, 237, 0.34);
  }

  .nav-right {
    display: flex;
    align-items: center;
    gap: 32px;
  }

  .nav-links {
    display: flex;
    gap: 36px;
  }

  .nav-resume {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.78rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    color: #fff;
    text-decoration: none;
    background: var(--accent);
    padding: 8px 18px;
    border-radius: 50px;
    transition: background 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease;
    white-space: nowrap;
  }

  .nav-resume:hover {
    background: #d44f20;
    transform: translateY(-1px);
    box-shadow: 0 4px 16px rgba(232, 92, 44, 0.35);
  }

  .nav-links a {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem;
    font-weight: 500;
    color: rgba(237, 237, 237, 0.5);
    text-decoration: none;
    letter-spacing: 0.02em;
    transition: color 0.2s ease;
  }

  .nav-links a:hover {
    color: rgba(237, 237, 237, 0.95);
  }

  @media (max-width: 480px) {
    .nav-logo-text { display: none; }
    .nav-avatar {
      width: 40px;
      height: 40px;
    }
    .nav-links { gap: 20px; }
    .nav-links a { font-size: 0.8rem; }
  }
</style>
